[build-system]
requires = ["setuptools>=77.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "stella-ai-agent-sdk"
version = "0.5.0"
description = "SDK for building agents that integrate with the STELLA platform"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
keywords = ["stella", "agent", "conversational-ai", "livekit", "grpc"]
authors = [
    {name = "STELLA Team"}
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "grpcio>=1.60.0",
    "grpcio-tools>=1.60.0",
    "pydantic>=2.0.0",
    "livekit>=0.11.0,<1.0.0",
    "PyJWT>=2.0.0",
    "numpy>=1.24.0",
    "aiohttp>=3.9.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
]
examples = [
    "openai>=1.0.0",
]

[project.urls]
Homepage = "https://github.com/c4dhi/STELLA/tree/main/agents/stella-ai-agent-sdk"
Documentation = "https://github.com/c4dhi/STELLA/tree/main/agents/stella-ai-agent-sdk#readme"
Source = "https://github.com/c4dhi/STELLA"
Issues = "https://github.com/c4dhi/STELLA/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"stella_agent_sdk" = ["py.typed"]
"stella_agent_sdk._grpc" = ["*.py"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
