{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",
  "packages": {
    ".": {
      "release-type": "node",
      "versioning": "always-bump-minor",
      "changelog-path": "CHANGELOG.md",
      "include-component-in-tag": false,
      "extra-files": [
        "CITATION.cff",
        "README.md"
      ]
    }
  },
  "$comment": "always-bump-minor: every release from main is a sprint release, 0.3.0 -> 0.4.0 -> 0.5.0. The agents' pyproject.toml files are deliberately NOT listed: they carry independent versions (0.5.0, 0.1.0, 0.1.0) and are not the project version."
}
